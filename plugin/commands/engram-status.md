# Engram Status — Show vault statistics

```bash
python3 /workplace/qianheng/ooda-memory/engram_cli.py status --vault ~/.engram/vault
```

Report the results: node count, edge count, daily notes, patterns, pending sessions.
