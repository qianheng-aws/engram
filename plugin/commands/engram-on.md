# Engram On — Enable auto-capture on session end

```bash
mkdir -p ~/.engram/vault/_meta && touch ~/.engram/vault/_meta/hook-enabled && echo "✅ Engram hook enabled — sessions will be queued for /engram replay"
```
